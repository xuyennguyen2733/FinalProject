#version 330 core

layout(location=0) in vec3 pos;
layout(location=1) in vec2 txc;

uniform mat4 mvp;
uniform mat4 mv;
uniform mat3 mn;

out vec3 position;
out vec2 texCoord;

void main()
{
	gl_Position = mvp * vec4(pos,1);
	position = pos;
	texCoord = txc;
}